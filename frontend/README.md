# Frontend React + Vite

## Chay local

```powershell
cd frontend
npm install
npm run dev
```

Mo `http://localhost:5173`.

Backend mac dinh: `http://localhost:8000`. Neu can doi:

```powershell
$env:VITE_API_BASE="http://localhost:8000"
npm run dev
```

